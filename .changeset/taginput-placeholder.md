---
"@bopoppy/core-ui": patch
---

`TagInput` now keeps its placeholder visible after tags are added, matching the design (which always shows "Add a tag…"). Previously the placeholder was hidden once any tag existed.
