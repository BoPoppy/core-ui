---
"@bopoppy/core-ui": minor
---

Add `Textarea` and `TextareaField` for multi-line text entry, completing the Text Field section from the design. `TextareaField` mirrors `TextField` (label/hint/error/success wiring, `aria-describedby` + `aria-invalid`) and the bare `Textarea` shares the input token styling with a resizable `min-height`.
